import styled from "styled-components"

const StyledGasStation = styled.div`
    
`

function GasStation() {
    return (
        <StyledGasStation className="GasStation">
            <h2 className='GasStation-title'>GasStation</h2>
        </StyledGasStation>
    )
}

export default GasStation
